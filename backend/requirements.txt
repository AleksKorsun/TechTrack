# Django (последняя версия)
Django==5.1.1

# Django REST Framework для API
djangorestframework==3.14.0

# CORS Headers для разрешения взаимодействия фронтенда с бэкендом
django-cors-headers==3.14.0

# JWT аутентификация для работы с токенами
djangorestframework-simplejwt==5.2.2

# Драйвер для PostgreSQL
psycopg2-binary  # Версия автоматически подберется в Dockerfile

# WSGI сервер для продакшн-окружения
gunicorn  # Версия автоматически подберется в Dockerfile

# Переменные окружения для управления настройками
django-environ  # Версия автоматически подберется в Dockerfile

# Sentry SDK для отслеживания ошибок (опционально)
sentry-sdk  # Версия автоматически подберется в Dockerfile

# Дополнительные библиотеки, используемые в проекте
asgiref==3.8.1
cffi==1.17.1
channels==4.1.0
geographiclib==2.0
geopy==2.3.0
greenlet==3.1.0
h3==3.7.7
numpy==2.1.1
pycparser==2.22
PyJWT==2.9.0
pytz==2024.2
setuptools==74.1.2
SQLAlchemy==2.0.17
sqlparse==0.5.1
timezonefinder==6.5.2
typing_extensions==4.12.2


